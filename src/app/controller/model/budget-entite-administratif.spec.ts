import { BudgetEntiteAdministratif } from './budget-entite-administratif';

describe('BudgetEntiteAdministratif', () => {
  it('should create an instance', () => {
    expect(new BudgetEntiteAdministratif()).toBeTruthy();
  });
});
